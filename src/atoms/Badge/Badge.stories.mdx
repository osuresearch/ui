
import { Canvas, Meta, Story, Description, ArgsTable } from '@storybook/addon-docs';

import { Badge } from '.';

<Meta title="Atoms/Badge" component={Badge} />

export const Template = (args) => <Badge {...args}>{args.label}</Badge>;

# Badge

<Description of={Badge} />

<Story
  name="Basic"
  args={{
    label: 'default badge',
  }}
  argTypes={{
    theme: {
      control: 'radio',
      options: [
        'primary',
        'secondary',
        'info',
        'success',
        'warning',
        'danger'
      ],
    }
  }}
>
  {Template.bind({})}
</Story>

<ArgsTable of={Badge} />

<Canvas>
  <Story name="Themes">
    <Badge theme="primary">primary</Badge>
    <Badge theme="secondary">secondary</Badge>
    <Badge theme="info">info</Badge>
    <Badge theme="success">success</Badge>
    <Badge theme="warning">warning</Badge>
    <Badge theme="danger">danger</Badge>
  </Story>
</Canvas>
