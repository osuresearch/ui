/// <reference types="react" />
declare type Props = {
    searching: boolean;
    error: boolean;
};
/**
 * Font Awesome icon to display inline with the input.
 *
 * Icon changes depending on the current input state
 * (searching, has results, has error, etc)
 *
 * @return {JSX.Element}
 */
export default function PrefixIcon({ searching, error }: Props): JSX.Element;
export {};
//# sourceMappingURL=PrefixIcon.d.ts.map